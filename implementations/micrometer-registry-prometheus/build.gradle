dependencies {
    api project(':micrometer-core')

    api 'io.prometheus:simpleclient_common'

    testImplementation project(':micrometer-test')
	
	api 'io.opentelemetry:opentelemetry-api:1.11.0'
}
