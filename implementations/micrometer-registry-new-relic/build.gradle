apply plugin: 'org.junit.platform.gradle.plugin'

dependencies {
    compile project(':micrometer-core')
    compile 'com.fasterxml.jackson.core:jackson-databind:latest.release' lock '2.9.4'

    testCompile project(':micrometer-test')
}