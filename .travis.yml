language: java
jdk:
- oraclejdk8
install: true
script: ./gradle/buildViaTravis.sh
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
before_install:
- test $TRAVIS_PULL_REQUEST = false && openssl aes-256-cbc -K $encrypted_f9867d26bacb_key -iv $encrypted_f9867d26bacb_iv -in gradle.properties.enc -out gradle.properties -d || true
notifications:
  webhooks:
    on_success: always
    on_failure: always
    on_start: false